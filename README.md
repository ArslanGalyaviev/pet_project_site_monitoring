# Site Monitoring Service

Асинхронный микросервис для мониторинга доступности сайтов с уведомлениями по email.

## Возможности

- **Асинхронная проверка** сайтов с настраиваемой периодичностью
- **Сохранение состояния** в SQLite базе данных (счетчик ошибок, статус алертов)
- **Защита от спама**: уведомление отправляется только после 5 последовательных ошибок
- **Email-уведомления** через SMTP (Яндекс.Почта)
- **Модульные тесты** с pytest

## Технологии

- Python 3.12+
- asyncio, httpx (асинхронные HTTP-запросы)
- SQLModel + aiosqlite (ORM и база данных)
- aiosmtplib (асинхронная отправка email)
- pytest + pytest-asyncio (тестирование)

## Архитектура
```
pet_project_site_monitoring/
├── app/
│   ├── __init__.py
│   ├── config.py          # Конфигурация и загрузка переменных окружения
│   ├── database.py        # Инициализация асинхронного движка БД
│   ├── models.py          # SQLModel модель данных Site
│   ├── monitor.py         # Асинхронные воркеры для проверки сайтов
│   └── notifier.py        # Асинхронная отправка email-уведомлений
├── tests/                 # Unit-тесты для проверки логики
├── benchmark/             # Скрипты для сравнения производительности HTTP-клиентов
├── .env                   # Секреты (игнорируются Git)
├── .gitignore
├── main.py                # Точка входа в приложение
├── pytest.ini             # Конфигурация pytest для асинхронных тестов
└── requirements.txt       # Зависимости проекта
```
## Тестирование 
pytest -v

## Как это работает
1. При запуске создается SQLite База данных (monitoring.db) с таблицей Site
2. Если БД пуста - заполняется дефолтным списком сайтов
3. Для каждого сайта запускается асинхронный воркер с индвидуальным интервалом
4. При успехе (2xx, 3xx) счетчик ошибок сбрасывается
5. При ошибке (сеть, 4xx, 5xx) счетчик consecutive_errors растет
6. На 5-ой ошибке: сохраняется в БД (alert_sent=True) и отправляется email
7. Следующие ошибки не шлют письма, пока сайт не начнет работать (сброс флага)
